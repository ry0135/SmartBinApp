<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Màu nền -->
    <solid android:color="#4CAF50"/>  <!-- xanh lá, bạn có thể đổi -->

    <!-- Kích thước -->
    <size
        android:width="60dp"
        android:height="60dp"/>

    <!-- Viền (nếu cần) -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF"/>
</shape>
